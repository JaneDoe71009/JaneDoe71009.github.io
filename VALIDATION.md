# Validation and remaining launch work

## Completed locally

- 41 automated checks passed on September 9, 2026, including calendar-source consensus and spaced PDF date parsing, plus live-countdown checks for minute/second precision, target time, legacy plans, excluded dates, and Eastern daylight-saving transitions. Auth calls are mocked; no live test emails or test accounts were created by the tests.
- TypeScript check passed.
- Production static export passed: 13 working app pages plus a not-found page.
- All 337 local page/asset references in the exported pages resolved to files. The publishing preparation step repeats this check.
- The live current-year school calendar was fetched and parsed successfully. The official IB hub returned HTTP 403, so the May 2027 final timetable was checked through three independent IB-school hosts. All three downloads were byte-for-byte identical and produced the same 18 exam days, from April 23 through May 18. The updater requires at least two matching parsed schedules and retains the previous snapshot if no majority exists.
- Navy/light-blue/white theme tokens are applied to both modes. Native page links support static GitHub Pages navigation without a dynamic routing server.
- A cold-preview calendar import failure was fixed by moving the canonical official calendar JSON from the static public folder into `lib`; the refresh script and publishing workflow follow the same file. Its dates were unchanged. Production export, types, and all 39 tests passed again. The signed-in calendar was then observed rendering the existing Freedom countdown with days, hours, minutes, and seconds; no saved plan was changed during this check.

## Published configuration

- The privacy-safe source was uploaded to the public GitHub user-site repository on September 9, 2026. GitHub Pages uses the custom `Publish IB Info` workflow, and the live home, account, calendar, and administration routes returned HTTP 200.
- The public repository description and source contain no school or district name, school URL, city/state label, private account identifier, or private student data. The school calendar URL and allowed source hosts are kept in repository Actions settings for the owner and collaborators rather than in committed files.
- A manual live refresh successfully read the configured school source, retained only generic school-calendar labels and dates in the repository, passed the build workflow, and published. Ordinary pushes do not write calendar status commits, preventing refresh loops; scheduled and manual refreshes can retain verified changes.
- Both public Supabase Actions variables were saved and verified. The Supabase Site URL is the public GitHub Pages origin, while both public and local `/account` return-address variants remain allowed.

## Remaining launch checks
- The live Supabase project was connected on September 5, 2026. The initial migration was applied after the owner approved its access rules; Supabase's additional RLS-protection option was selected. The setup and verification query were saved privately in its SQL editor.
- All seven live preflight checks passed: three application tables, public-table RLS, denied reads on private tables, inaccessible owner bootstrap for visitors, no identity columns in public content, seven intended access policies, and no prematurely assigned owner/admin.
- The browser-safe publishable key is configured in ignored `.env.local`. Anonymous requests returned HTTP 200 for the empty public content feed and HTTP 401 for personal plans and reports. The connected account page returned HTTP 200 and the sign-in form, and the connected production build passed.
- Password sign-in replaced the magic-link/code form on September 5, 2026. Live settings were read: email provider enabled, new signups enabled, email confirmation enabled, and custom SMTP disabled. No authentication security settings were changed. The user reported a sender rate limit and an expired/invalid email link; this code change does not claim to lift the limit. A user-entered password signup/sign-in and recovery round-trip remain to be verified after email delivery is available.
- On September 6, at the user's explicit request, Confirm email was switched off and saved in the live project. The read-only Auth settings endpoint returned HTTP 200 with emailAutoConfirm=true, emailEnabled=true, and signupDisabled=false. Signup copy and actions now match this configuration: no verification-resend button and no claim that a verification email was sent. Password recovery still requires email. Supabase auto-confirms new signups, so the existing membership rules continue to apply without a database migration.
- The account page was checked directly after the reported connection failure and returned HTTP 200; the running listener was confirmed on 127.0.0.1:3000. A reusable independent preview launcher was added for future restarts; it currently detects and reuses the existing server. The browser was still on the removed resend form after its live update, and was returned to the supported sign-in form. No real password was entered or changed by the agent.
- An attempted check for older pending accounts was blocked by automatic privacy review because the Users list could reveal unrelated email identities. It was not retried through another route. Existing-account activation/password state and a real signup round-trip are not claimed as verified.
- At the user's later explicit request, the previous account was deleted and the user created a replacement account. The replacement was configured as the sole owner, verified in the live database. The user's private plan and countdown were then observed loading in the site; they were not reset by the countdown update. Operational logs were not purged. No private account identifiers are included in this repository.
- No custom email-delivery provider is configured. New signup no longer needs email, but recovery still requires an appropriate sender and the live launch checks in SETUP.md.
- The optional read-only WebMCP calendar tool registered with the intended public-data schema and annotations. Execution validation could not complete because the browser reported a stale/unavailable context. It is not claimed as verified; this optional agent tool is not required for students to use the website.
- Broad browser interaction, mobile rendering, and visual QA were not performed. The local preview was opened and restored successfully; a real multi-account launch check remains required.

There is no claim that a word filter prevents every abusive message or that infrastructure access is untraceable. Human moderation and the documented privacy limits still apply.
